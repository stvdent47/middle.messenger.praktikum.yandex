{
  "name": "middle.messenger.praktikum.yandex",
  "version": "1.0.0",
  "description": "### Откройте pull request в ветку main из ветки, где вы разрабатывали проект, и добавьте ссылку на этот pr в README.md в ветке main.  ### ВАЖНО: pull request должен называться “Sprint i” (i — номер спринта).",
  "scripts": {
    "dev": "parcel src/pages/login.hbs --no-cache && parcel watch ./*",
    "build": "rm -rf dist && parcel build src/pages/login.hbs --no-cache --public-url ./",
    "start": "rm -rf dist && parcel build src/pages/login.hbs --no-cache --public-url ./ && node ./src/app.js"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/stvdent47/middle.messenger.praktikum.yandex.git"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@anikin/parcel-transformer-handlebars": "^1.0.9",
    "express": "^4.18.1",
    "parcel": "^2.6.0",
    "postcss": "^8.4.14",
    "postcss-nested": "^5.0.6"
  },
  "engines": {
    "npm": ">=8.11.0",
    "node": ">=16.15.1"
  }
}

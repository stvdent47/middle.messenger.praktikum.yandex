import { Button } from './Button';
import { Input, InputError, InputField } from './InputField';
import { Link } from './Link';

export const ComponenstUI = [Button, InputField, Input, InputError, Link];
